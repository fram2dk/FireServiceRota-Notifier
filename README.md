# FireServiceRota-Notifier


at initial install run /init.py to start with example/initial files.

#FSR_telegram
minimum set token in /compose.yaml or /runtime/FSR_telegram/data/config.json

#FSR_listener
user credentials is needed for connection to fireservicerota
edit /runtime/FSR_listener/config.json

minimum: set username and password
optional: set dictionary for vehicle number => name
